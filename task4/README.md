# Прогнозирование временных рядов - Task4

Полный пайплайн для прогнозирования временных рядов с использованием современных методов машинного обучения.

## Структура проекта

```
Task4/
├── feature_engineering.py    # Этап 1: Инжиниринг признаков
├── validation.py              # Этап 2: Валидация и разбиение
├── hyperparameter_tuning.py   # Этап 3: Подбор гиперпараметров
├── forecasting_strategies.py  # Этап 4: Стратегии прогнозирования
├── models.py                  # Этап 5: Построение моделей
├── diagnostics.py             # Этап 6: Диагностика моделей
├── evaluation.py              # Этап 7: Оценка качества
├── advanced_techniques.py     # Этап 8: Продвинутые техники
├── run_pipeline.py            # Главный скрипт
├── requirements.txt           # Зависимости
└── results/                   # Результаты (создается автоматически)
```

## Установка

### Базовая установка

```bash
pip install -r requirements.txt
```

### Установка с AutoGluon (рекомендуется)

AutoGluon - мощная AutoML платформа для временных рядов, которая автоматизирует весь процесс прогнозирования.

**Требования:**
- Python 3.9-3.12
- ~2GB свободного места на диске
- Рекомендуется: GPU для ускорения обучения глубоких моделей

```bash
pip install autogluon.timeseries
```

Или раскомментируйте строку в `requirements.txt`:
```bash
# autogluon.timeseries>=0.7.0  # <- уберите #
```

**⚠️ Если AutoGluon не подходит для вашей системы:**
См. раздел "Альтернативы AutoGluon" ниже и файл `INSTALL_ALTERNATIVES.md`.

## Использование

### Базовый запуск

```bash
python run_pipeline.py
```

Скрипт автоматически:
1. Загружает данные из `../New_final.csv`
2. Создает признаки (лаги, окна, сезонность и т.д.)
3. Разбивает данные на train/val/test
4. Подбирает гиперпараметры
5. Обучает модели
6. Проводит диагностику
7. Оценивает качество
8. Создает ансамбли
9. Генерирует итоговый отчет

### Веб-интерфейс для сравнения моделей

Для интерактивного сравнения моделей используйте веб-интерфейс:

```bash
streamlit run web_interface.py
```

Веб-интерфейс позволяет:
- 📈 Просматривать метрики всех моделей в удобной таблице
- 📉 Визуализировать метрики на графиках
- 🔮 Сравнивать прогнозы топ-5 моделей
- 🎯 Анализировать важность признаков
- 📊 Просматривать результаты Diebold-Mariano теста
- 💾 Экспортировать результаты

Интерфейс автоматически запускает упрощенный пайплайн и показывает результаты сравнения моделей.

### Результаты

Все результаты сохраняются в директории `results/`:
- `metrics.csv` / `metrics.json` - метрики всех моделей
- `metrics_with_autogluon.csv` / `metrics_with_autogluon.json` - метрики с AutoGluon
- `dm_test.csv` - результаты Diebold-Mariano теста
- `acf_*.png` - ACF графики остатков
- `feature_importance_*.png` - важность признаков
- `top3_models.png` - визуализация топ-3 моделей
- `summary.json` - итоговый отчет
- **AutoGluon результаты:**
  - `autogluon_leaderboard_*.csv` - таблица лидеров для каждого пресета
  - `autogluon_backtesting_*.csv` - результаты backtesting валидации
  - `autogluon_vs_custom.csv` - сравнение AutoGluon с кастомными моделями
  - `autogluon_recommendations.json` - рекомендации по использованию
  - `autogluon_presets_comparison.csv` - сравнение пресетов AutoGluon

## 🚀 AutoGluon-TimeSeries Integration

### Что такое AutoGluon?

AutoGluon-TimeSeries - современная AutoML платформа от Amazon, которая **автоматически**:
- 🔧 Генерирует признаки из временных рядов
- 🎯 Подбирает оптимальные гиперпараметры
- 🏗️ Строит ансамбли моделей (включая DeepAR, TFT, SimpleFeedForward)
- 📊 Проводит backtesting валидацию
- 🏆 Создает таблицу лидеров (leaderboard)

### Преимущества AutoGluon

✅ **Качество "из коробки"**: часто превосходит ручные модели без настройки  
✅ **Экономия времени**: нет необходимости в ручном feature engineering  
✅ **Продвинутые модели**: доступ к глубоким нейронным сетям (DeepAR, Temporal Fusion Transformer)  
✅ **Встроенный ансамблинг**: автоматическое объединение нескольких моделей  
✅ **Backtesting**: встроенная временная валидация

### Как работает интеграция?

Пайплайн автоматически:
1. **Обучает AutoGluon** с несколькими пресетами качества:
   - `medium_quality`: баланс скорость/качество (5 минут)
   - `high_quality`: высокое качество (10-15 минут)
   - `best_quality`: максимальное качество (опционально, >30 минут)

2. **Проводит backtesting валидацию** с 3 окнами

3. **Сравнивает с кастомными моделями** по критериям:
   - 📊 Качество (MAE, RMSE, MASE)
   - ⏱️ Скорость обучения
   - 🔍 Интерпретируемость
   - 🛠️ Гибкость настройки
   - 🤖 Уровень автоматизации

4. **Генерирует рекомендации**:
   - Когда использовать AutoGluon
   - Когда использовать кастомные модели
   - Стратегия для продакшена
   - Шаблон переобучения

### Пресеты AutoGluon

| Пресет | Время обучения | Качество | Модели | Рекомендуется |
|--------|---------------|----------|--------|---------------|
| `fast_training` | ~2 мин | Базовое | 3-5 | Прототипирование |
| `medium_quality` | ~5 мин | Хорошее | 5-10 | **MVP, быстрая разработка** ✅ |
| `good_quality` | ~10 мин | Очень хорошее | 10-15 | Боевые приложения |
| `high_quality` | ~15 мин | Высокое | 15-20 | **Критичные задачи** ✅ |
| `best_quality` | >30 мин | Максимальное | 20+ | Финальная оптимизация |

### Интерпретация результатов

После запуска пайплайна вы получите:

#### 1. Leaderboard (таблица лидеров)
```
📊 Leaderboard для medium_quality:
                     model  score_val
0  WeightedEnsemble        0.1234
1  DeepAR                  0.1256
2  TemporalFusionTrans...  0.1289
```

**Интерпретация:** 
- `score_val` - метрика на валидации (MAE)
- Меньше = лучше
- WeightedEnsemble обычно лучший (объединяет все модели)

#### 2. Сравнение AutoGluon vs Custom
```
                model    type    MAE   RMSE  training_time  interpretability
0  AutoGluon_high_q.  AutoML  245.3  312.1          856.2               Low
1  LightGBM_tuned    Custom  251.7  318.5           42.3              High
```

**Интерпретация:**
- Если AutoGluon MAE **<5% лучше** → использовать AutoGluon ✅
- Если Custom MAE **<5% лучше** → использовать Custom ✅
- Если разница **<5%** → гибридная стратегия 🤝

#### 3. Рекомендации
Файл `autogluon_recommendations.json` содержит:
- `summary`: краткая сводка лучших моделей
- `use_autogluon_when`: когда предпочтительнее AutoGluon
- `use_custom_when`: когда предпочтительнее кастомные модели
- `production_strategy`: рекомендуемая стратегия для продакшена
- `retraining_template`: шаблон для переобучения

### AutoGluon vs Кастомные модели: Когда что использовать?

#### ✅ Используйте AutoGluon когда:
- 🚀 Нужен быстрый MVP или proof-of-concept
- 🔧 Ограничены ресурсы на feature engineering
- 🎯 Требуется автоматический подбор гиперпараметров
- 🏆 Нужны state-of-the-art модели (DeepAR, TFT)
- 📊 AutoGluon показал >5% преимущество в качестве

#### ✅ Используйте кастомные модели когда:
- 🔍 Требуется полная интерпретируемость (LIME, SHAP)
- 🛠️ Нужен контроль над каждым этапом
- ⚙️ Специфические требования к preprocessing
- 💾 Ограничения по памяти/размеру модели
- 📈 Кастомные модели показали >5% преимущество

#### 🤝 Гибридная стратегия (рекомендуется):
1. **Phase 1 (MVP)**: AutoGluon для быстрого запуска
2. **Phase 2 (Анализ)**: Изучить feature importance из AutoGluon
3. **Phase 3 (Оптимизация)**: Разработать кастомные модели на основе инсайтов
4. **Phase 4 (A/B тест)**: Сравнить оба подхода в продакшене

### Продакшен: Шаблон переобучения

```python
# Еженедельное переобучение или при деградации MASE >10%
1. Загрузить новые данные
2. Обновить feature engineering pipeline
3. Переобучить AutoGluon с best_preset из валидации
4. Переобучить топ-3 кастомных модели
5. Сравнить на validation set
6. Задеплоить лучшую модель
7. Мониторить MASE/MAE на production данных
8. При деградации >10% → return to step 1
```

### Troubleshooting AutoGluon

**Проблема**: `ImportError: cannot import name 'TimeSeriesPredictor'`  
**Решение**: Установите `pip install autogluon.timeseries` (не `autogluon`)

**Проблема**: `Python version not supported (3.8 or 3.13)`  
**Решение**: Используйте Python 3.9-3.12 или см. альтернативы ниже

**Проблема**: `Out of memory`  
**Решение**: Уменьшите `time_limit_per_preset` в `run_pipeline.py`

**Проблема**: `Training takes too long`  
**Решение**: Используйте только `fast_training` или `medium_quality` пресеты

### Пример использования AutoGluon API

Модуль `autogluon_integration.py` предоставляет удобный wrapper:

```python
from autogluon_integration import AutoGluonWrapper

# Создать wrapper
ag = AutoGluonWrapper(prediction_length=7, eval_metric="MAE")

# Подготовить данные
train_data = ag.prepare_data(series, date_index)

# Обучить с несколькими пресетами
ag.fit_multiple_presets(train_data, presets=["medium_quality", "high_quality"])

# Получить прогнозы
predictions = ag.predict(preset="high_quality")

# Получить leaderboard
leaderboard = ag.get_leaderboard("high_quality")

# Backtesting
results = ag.backtesting(full_data, num_windows=3)

# Сравнить пресеты
comparison = ag.compare_presets()
```

## Этапы пайплайна

### Этап 1: Инжиниринг признаков
- Лаги: 1, 2, 3, 7, 14, 30
- Скользящие окна: mean, std, min, max, median (окна 7, 14, 30)
- Экспоненциальное сглаживание: α = 0.3, 0.5, 0.7
- Временные признаки: day_of_week, month, quarter, week_of_year
- Сезонные компоненты: ряды Фурье для периодов 7 и 30
- Трансформации: логарифм, Бокс-Кокс

### Этап 2: Валидация и разбиение
- Хронологическое разбиение: 60% train, 20% val, 20% test
- TimeSeriesSplit (n_splits=5, max_train_size=365)
- Purged Walk-Forward с gap
- Поддержка AutoGluon backtesting

### Этап 3: Подбор гиперпараметров
- GridSearchCV для линейных моделей
- Optuna для градиентного бустинга (LightGBM, XGBoost)
- AutoGluon автоматический подбор

### Этап 4: Стратегии прогнозирования
- **Direct**: h отдельных моделей
- **Recursive**: рекурсивное предсказание
- **Multi-output**: одна модель для всех горизонтов
- **DirRec**: гибридная стратегия

### Этап 5: Построение моделей
- **Линейные**: Linear, Ridge, Lasso, ElasticNet, BayesianRidge
- **Ансамбли**: RF, ExtraTrees, XGBoost, LightGBM, CatBoost
- **SVM**: SVR, NuSVR
- **Бейзлайны**: Naive, Seasonal Naive, MA, Linear Trend

### Этап 6: Диагностика моделей
- Анализ остатков: ACF, ADF/KPSS тесты
- Feature importance: SHAP, встроенные веса
- Визуализация: факт vs прогноз, ошибки по времени

### Этап 7: Оценка качества
- Метрики: MAE, RMSE, MAPE, MASE, RMSSE
- Diebold-Mariano тест для сравнения моделей
- Сводная таблица результатов

### 🚀 Этап 7.5: AutoGluon-TimeSeries (новое!)
- **Обучение с множественными пресетами**: medium_quality, high_quality, best_quality
- **Автоматическая генерация признаков** и подбор гиперпараметров
- **Backtesting валидация** с несколькими временными окнами
- **Leaderboard** с ранжированием всех внутренних моделей AutoGluon
- **Сравнение** AutoGluon vs кастомные модели по 5 критериям
- **Генерация рекомендаций** по использованию в продакшене
- **Сравнение пресетов** по качеству и времени обучения

### Этап 8: Продвинутые техники
- Ансамблирование: взвешенное усреднение, stacking
- Обработка выбросов: Isolation Forest, Winsorization, RobustScaler
- Сегментация: кластеризация, сезонная сегментация

### Этап 9: Итоговый анализ
- Ранжирование моделей по MASE
- Визуализация лучших моделей
- Рекомендации по продакшену

## Настройка параметров

В файле `run_pipeline.py` можно изменить:
- `DATA_PATH` - путь к данным
- `TARGET_COL` - название целевой переменной
- `DATE_COL` - название колонки с датами
- `HORIZON` - горизонт прогнозирования
- `RESULTS_DIR` - директория для результатов

## Примечания

- 🚀 **AutoGluon интегрирован в пайплайн!** (Этап 7.5) - см. раздел выше
- AutoGluon требует Python 3.9-3.12 и ~2GB места для установки
- Если AutoGluon недоступен, пайплайн автоматически пропустит этап 7.5
- Для больших данных рекомендуется уменьшить `time_limit_per_preset` в `run_pipeline.py`
- SHAP анализ может быть медленным на больших выборках
- Рекомендуется запускать на машине с ≥8GB RAM для AutoGluon

## Альтернативы AutoGluon

Если AutoGluon не подходит под вашу версию Python или систему, см. файл `INSTALL_ALTERNATIVES.md` для инструкций по установке альтернативных библиотек:

| Библиотека | Установка | Скорость | Качество | Рекомендуется |
|-----------|-----------|----------|----------|---------------|
| **StatsForecast** | `pip install statsforecast` | 🚀🚀🚀 Очень быстро | ⭐⭐⭐ Хорошее | ✅ Да |
| **Darts** | `pip install darts` | 🚀🚀 Быстро | ⭐⭐⭐⭐ Отличное | ✅ Да |
| **Prophet** | `pip install prophet` | 🚀 Медленно | ⭐⭐⭐ Хорошее | ⚠️ Требует C++ |
| **Sktime** | `pip install sktime[all]` | 🚀🚀 Быстро | ⭐⭐⭐ Хорошее | ✅ Да |
| **AutoGluon** | `pip install autogluon.timeseries` | 🚀 Медленно | ⭐⭐⭐⭐⭐ Превосходное | ✅✅ **Лучшее для продакшена** |

**Рекомендация**: 
- Для прототипирования: **StatsForecast** (самая быстрая)
- Для продакшена: **AutoGluon** (лучшее качество)
- Для баланса: **Darts** (универсальность)

## FAQ

### Что делать, если AutoGluon не устанавливается?
1. Проверьте версию Python: `python --version` (должна быть 3.9-3.12)
2. Если версия не подходит - используйте альтернативы (StatsForecast, Darts)
3. Пайплайн работает и без AutoGluon - просто пропустит этап 7.5

### Какой пресет AutoGluon выбрать?
- **MVP/прототип**: `medium_quality` (5 мин)
- **Продакшен**: `high_quality` (15 мин)
- **Максимальное качество**: `best_quality` (>30 мин, только если критично)

### Как ускорить обучение AutoGluon?
В `run_pipeline.py` измените:
```python
time_limit_per_preset = 120  # 2 минуты вместо 5
presets_to_test = ["medium_quality"]  # только один пресет
```

### AutoGluon vs кастомные модели - что лучше?
Запустите пайплайн и посмотрите файл `autogluon_vs_custom.csv`. Пайплайн автоматически даст рекомендацию в `autogluon_recommendations.json`.

**В среднем:**
- AutoGluon даёт **на 5-15% лучше качество** на сложных рядах
- Кастомные модели **быстрее обучаются** (в 10-20 раз)
- Кастомные модели **более интерпретируемы**
- Рекомендуется **гибридная стратегия**: AutoGluon для MVP → кастомные для оптимизации

## Лицензия

MIT

